enum RouteCodes {
  register = "/register",
  migrate = "/migrate",
  invite = "/:key",
  faucets = "/faucets",
  dataSharingRules = "/data-sharing-rules",
  bbRules = "bb-rules",
}

export default RouteCodes;
